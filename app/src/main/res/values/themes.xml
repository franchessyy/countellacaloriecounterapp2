<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Day Mode Theme -->
    <style name="Theme.Countella" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary and secondary colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item> <!-- Correct reference -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>

        <!-- Background color -->
        <item name="android:windowBackground">@color/backgroundColor</item>

        <!-- Customize Status Bar and Navigation Bar colors for day mode -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/colorPrimary</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/colorSecondary</item>
    </style>

</resources>
